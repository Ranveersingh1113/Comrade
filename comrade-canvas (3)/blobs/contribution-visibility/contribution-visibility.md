# Contribution visibility

Task-first view, visible to all team members by default. Each member shows assigned tasks with a completion bar. GitHub activity adds weight where relevant. No rankings, no activity feed in group view. Sensitive signals (e.g. member gone quiet) are handled privately via the AI thread, not surfaced to the group.